#include "funcs.h"

int main()
{
    User mat[MAX_ROW][MAX_COL];
    int counts[8] = {};
    int row, col;
    printf("Filas: ");
    scanf("%d", &row);
    printf("Columnas: ");
    scanf("%d", &col);
    
    return 0;
}